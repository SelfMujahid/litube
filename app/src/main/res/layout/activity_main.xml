<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    tools:context=".MainActivity">

    <!-- Fullscreen content area (swipeRefresh + webview) -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.hhst.youtubelite.webview.YoutubeWebview
            android:id="@+id/webview"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ProgressBar
                android:id="@+id/progressBar"
                style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:indeterminateOnly="false"
                android:progressDrawable="@drawable/progress_bar"
                android:visibility="gone"
                android:max="100" />

        </com.hhst.youtubelite.webview.YoutubeWebview>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Mini player overlay: initially GONE. We will animate webview itself,
         but this view holds mini controls (play/pause/title) that appear in mini mode. -->
    <FrameLayout
        android:id="@+id/miniControls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:padding="6dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/mini_controls_bg">

        <!-- small thumbnail or icon (optional) -->
        <ImageView
            android:id="@+id/mini_thumb"
            android:layout_width="56dp"
            android:layout_height="40dp"
            android:scaleType="centerCrop"
            android:src="@mipmap/ic_launcher" />

        <!-- Play/Pause button on top right of mini controls -->
        <ImageButton
            android:id="@+id/mini_play_pause"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="end|center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_pause"
            android:contentDescription="@string/action_play" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
